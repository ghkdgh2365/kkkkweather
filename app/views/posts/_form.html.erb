<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-sm">
    </div>
    <div class="col-sm">
      <div class="field">
        <%= form.label :email, "이메일 주소", :style => "font-weight:bold" %>
        <%= form.text_field :email, :class => "form-control" %>
      </div>
    </div>
    <div class="col-sm">
    </div>
  </div>
  <div class="actions">
    <%= form.submit "신청", :class => "btn btn-primary btn-lg" %>
  </div><br>
<% end %>
